name: app_config
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  singleton: ^0.0.3-nullsafety
  crash_report:
    git:
      url: https://github.com/bookbot-kids/crash_report

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true